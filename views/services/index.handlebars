<nav class="navbar navbar-expand-lg navbar-dark info-color">
  <div class="container">
    <a class="navbar-brand" href="/">Time is Money</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        {{#each allSubCategories}}
          <li class="nav-item">
            <a class="nav-link" href="/services/{{../Category}}/{{ this.subcategory_name}}">{{this.subcategory_name}}</a>
          </li>
        {{/each}}
      </ul>
      <ul class="navbar-nav ml-auto nav-flex-icons">
        {{#if cur_user}}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i> {{cur_user.username}}</a>
          <div class="dropdown-menu dropdown-menu-right dropdown-unique" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="/profile">Profile</a>
            <a class="dropdown-item" href="#">Settings</a>
            <a class="dropdown-item" href="/logout">Log Out</a>
          </div>
        </li>
        {{ else }}
        <li class="nav-item">
          <a href="/signup" class="btn btn-light-green btn-sm wow fadeInRight" rel="nofollow">Sign Up</a>
        </li>
        <li class="nav-item">
          <a href="/login" class="btn btn-primary btn-sm wow fadeInRight" rel="nofollow">Log In</a>
        </li>
      {{/if}}
      </ul>
    </div>
  </div>
</nav>

<!-- Services -->
<div class="container-fluid">
  <div class="container">
    <div class="divider-new">
      <h1 class="h1-responsive font-bold text-center">{{SubCategory}}</h1>
    </div>
    <div class="card-deck">
      {{#each allServices}}
        <div class="col-md-4">
          <a href="/services/{{../Category}}/{{../SubCategory}}/{{this.id}}">
            <div class="card text-center">
              <div class="card-header teal accent-3">
                <h4 class="card-title font-bold text-white">{{this.service_name}}</h4>
              </div>
              <div class="card-body">
                <p class="card-text">{{this.description}}</p>
              </div>
            </div>
          </a>
        </div>
      {{/each}}
    </div>
  </div>
</div>
<!-- IE10 viewport hack for Surface/desktop Windows 8 bug -->
<script src="../../assets/js/ie10-viewport-bug-workaround.js"></script>
